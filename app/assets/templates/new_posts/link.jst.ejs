<form class="post-form">
<div class="fright cancel-post large-text"><span class="glyphicon glyphicon-remove-circle"></span></div>
<div class="new-post-title"><% if (post.id) { %>
	Edit Link Post.
	<%} else {%>
	New Link Post.
	<%}%></div>

	<div id="post-errors"></div>
	<input type="hidden" name="post[type]" value="Link">
		
	<input type="text" class="form-control" name="post[link]" value="<% if (post.get('link')){ %><%=post.get('link') %><% }%>" placeholder="Add your link here" />	
	
	<div class="optional-text" style="display:none">
		<div class="input-group">
			<span class="input-group-addon glyphicon glyphicon-font"></span>
			<input class="form-control"  type="text" name="post[title]" placeholder="Write the title here..." value="<% if (post.get('title')){ %>
	<%=		post.get('title') %>
  <% }%>">
		</div>
 
	<br>


	<textarea class="form-control" name="post[post_text]" value="" placeholder="Write your post's text here..."></textarea>
  </div>
	<br>
	<button class="btn btn-warning toggle-options fright">Show Advanced Options</button>
	<div id="adv-opt-clear-div" class="clear-div" style="display: none;"></div>
	<div class="advanced-options" style="display: none;">
	<br>
		<div class="input-group">
			<span class="input-group-addon glyphicon glyphicon-globe"></span>
			<input class="form-control" type="text" name="post[custom_url]" value="" placeholder="eg. /Amazing-Blog-Post" id="post-url">
		</div>

	
		<br>
		<div class="input-group">
			<span class="input-group-addon glyphicon glyphicon-link"></span>
			<input class="form-control" type="text" name="post[source_link]" placeholder="Source Link" value="">
    </div>
		
		<br>
		<div class="input-group">
			<span class="input-group-addon glyphicon glyphicon-calendar"></span>
			<input class="form-control" type="date" name="post[publish_date]" value="" placeholder="PUBLISH DATE">
		</div>
	<br>
	</div>

	<button class="btn btn-success <% if (post.id) { %>
	update-post
	<%} else {%>
	create-post
	<%}%>"><% if (post.id) { %>
	Update Post
	<%} else {%>
	Create Post
	<%}%></button>
</form>